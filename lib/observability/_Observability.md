# Observability

Reusable JSON-lines operational logging. It has no domain, persistence, transport, or runtime orchestration responsibility.

| Module | Purpose |
|---|---|
| `Logger` | Emit structured operational events with optional entity identifiers |
