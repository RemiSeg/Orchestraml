# Infrastructure

Production adapters for application ports. Depends on domain and application contracts; contains no business rules.

| Namespace | Scope |
|---|---|
| `Runtime` | Configuration, UTC clock, UUID generation |
| `Postgres` | Migrations, row mapping, pool, persistence |
